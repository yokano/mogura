/**
 * ランキング画面
 * @class
 * @extends Scene
 */
var RankingScene = Class.create(Scene, {
	/**
	 * 初期化
	 * @memberof RankingScene
	 * @method
	 */
	initialize: function() {
		Scene.call(this);
	},
	
	/**
	 * シーン開始時の処理
	 * @memberof RankingScene
	 * @method
	 */
	onenter: function() {
	}
});

var RankingBoard = Class.create();