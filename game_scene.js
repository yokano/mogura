/**
 * ゲームプレイ画面
 * @class
 * @extends Scene
 */
var GameScene = Class.create(Scene, {
	initialize: function() {
		Scene.call(this);
	},
	
	onenter: function() {
	}
});

/**
 * タイマー
 * @class
 * @extends Group
 */
var Timer = Class.create(Group, {
	
});

/**
 * もぐらを表すクラス
 * @class
 * @extends Sprite
 */
var Mole = Class.create(Sprite, {
	initialize: function() {
		
	}
});